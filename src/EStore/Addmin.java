package EStore;

public class Addmin extends User {

    public Addmin(String name, int age, String emailAddress, String homeAddress, String password, int phoneNumber) {
        super(name, age, emailAddress, homeAddress, password, phoneNumber);
    }
}
